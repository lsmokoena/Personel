@using Personel.ViewModel
@model ReviewsViewModel

@{
    ViewData["Title"] = "Reviews";
}
<div class="col-md-10 offset-md-1 white z-depth-1 py-1 pt-1">
    <div class="row">
        <div class="col-sm-6 col-md-9 py-1 px-1">
            @*if (User.FindFirst("Update Account Types") != null)
                {*@
            <a href="@Url.Action("Edit", "Review", new { id = 0 })" id="newuser" class="btn primary-color waves-effect waves-light">
                New Industry
            </a>
            @*}*@
        </div>
        <div class="col-sm-6 col-md-3">
            <div class="md-form">
                <input placeholder="Search..." type="text" id="searchfield" name="filter" value="" class="form-control input-underscore">
            </div>
        </div>
    </div>
    <div>
        <div class="row">
            <div class="col-md-12">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Level</th>
                                <th>Remark</th>
                                <th>Skill</th>
                                <th>Person</th>
                                <th>User</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody id="searchresult">
                            @foreach (var review in Model.Reviews)
                            {
                                <tr>
                                    <td>@review.Level</td>
                                    <td>@review.Remark</td>
                                    <td>@review.SkillId</td>
                                    <td>@review.PersonId</td>
                                    <td>@review.UserId</td>
                                    @*if (User.FindFirst("Update AccountType") != null)
                                        {*@
                                    <td>
                                        <a href="@Url.Action("Edit", "Review", new { id = review.ID })" class="btn primary-color btn-sm">Edit</a>
                                        <a href="#" onclick="" class="btn danger-color btn-sm">Delete</a>
                                    </td>
                                    @*}*@
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        @Html.Partial("~/Views/Shared/Pager.cshtml", Model.Pager)

    </div>
</div>